export interface MonthYear {
  month: number;
  year: number;
}

export interface BudgetCell {
  categoryId: string;
  month: number;
  year: number;
  value: number;
}

export interface CellPosition {
  categoryId: string;
  month: number;
  year: number;
}

